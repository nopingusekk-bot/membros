const {sql,json,body,requireAuth}=require('../_lib');
module.exports=async(req,res)=>{if(req.method!=='POST')return json(res,405,{error:'Método não permitido'});const u=await requireAuth(req,res);if(!u)return;try{const {lesson,completed}=await body(req);if(!Number.isInteger(lesson))return json(res,400,{error:'Aula inválida'});await sql`insert into progress(member_id,lesson,completed) values(${u.id},${lesson},${!!completed}) on conflict(member_id,lesson) do update set completed=excluded.completed,updated_at=now()`;return json(res,200,{ok:true})}catch(e){return json(res,500,{error:'Erro ao salvar progresso'})}};
