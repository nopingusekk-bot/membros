const {sql,json,body,requireAuth}=require('../_lib');
module.exports=async(req,res)=>{if(req.method!=='POST')return json(res,405,{error:'Método não permitido'});const u=await requireAuth(req,res);if(!u)return;try{const {lesson,rating}=await body(req);const r=Number(rating);if(!Number.isInteger(lesson)||r<1||r>5)return json(res,400,{error:'Avaliação inválida'});await sql`insert into ratings(member_id,lesson,rating) values(${u.id},${lesson},${r}) on conflict(member_id,lesson) do update set rating=excluded.rating,updated_at=now()`;return json(res,200,{ok:true})}catch(e){return json(res,500,{error:'Erro ao salvar avaliação'})}};
